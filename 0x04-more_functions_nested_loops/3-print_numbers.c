#include "main.h"

/**
 * print_numbers- print numbers between o to 9
 * return:0(success)
 */
void print_numbers(void)
{
	int ch;

	for (ch = 0; ch < 10; ch++)

	          	_putchar(ch + '0');

	_putchar('\n');

	return (0);
}
